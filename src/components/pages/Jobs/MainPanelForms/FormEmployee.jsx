
const FormEmployee = () => {
    const submitHandler = (e) => {
        e.preventDefault()
    }

  return (
    <div>
        <form onSubmit={submitHandler}>

        </form>
    </div>
  )
}

export default FormEmployee